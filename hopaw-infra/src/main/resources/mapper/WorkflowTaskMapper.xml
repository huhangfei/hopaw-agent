<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agent.hopaw.infra.mapper.WorkflowTaskMapper">

    <resultMap id="WorkflowTaskResultMap" type="com.agent.hopaw.infra.model.entity.WorkflowTask">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="status" column="status"/>
        <result property="projectId" column="project_id"/>
        <result property="agentId" column="agent_id"/>
        <result property="startTime" column="start_time"/>
        <result property="executionPeriod" column="execution_period"/>
        <result property="rejectReason" column="reject_reason"/>
        <result property="userId" column="user_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="allColumns">
        id, title, content, status, project_id, agent_id, start_time, execution_period,
        reject_reason, user_id, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.agent.hopaw.infra.model.entity.WorkflowTask">
        INSERT INTO workflow_tasks (title, content, status, project_id, agent_id, start_time, execution_period, reject_reason, user_id)
        VALUES (#{title}, #{content}, #{status}, #{projectId}, #{agentId}, #{startTime}, #{executionPeriod}, #{rejectReason}, #{userId})
        <selectKey resultType="long" keyProperty="id" order="AFTER">
            SELECT last_insert_rowid()
        </selectKey>
    </insert>

    <update id="update" parameterType="com.agent.hopaw.infra.model.entity.WorkflowTask">
        UPDATE workflow_tasks
        SET title = #{title},
            content = #{content},
            status = #{status},
            project_id = #{projectId},
            agent_id = #{agentId},
            start_time = #{startTime},
            execution_period = #{executionPeriod},
            reject_reason = #{rejectReason},
            user_id = #{userId},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM workflow_tasks WHERE id = #{id}
    </delete>

    <select id="findById" resultMap="WorkflowTaskResultMap">
        SELECT <include refid="allColumns"/>
        FROM workflow_tasks
        WHERE id = #{id}
    </select>

    <select id="findByUserIdAndStatus" resultMap="WorkflowTaskResultMap">
        SELECT <include refid="allColumns"/>
        FROM workflow_tasks
        WHERE user_id = #{userId}
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY id DESC
    </select>

    <select id="findPendingExecution" resultMap="WorkflowTaskResultMap">
        SELECT <include refid="allColumns"/>
        FROM workflow_tasks
        WHERE status = 'pending_execution'
        ORDER BY id ASC
    </select>

    <update id="updateStatus">
        UPDATE workflow_tasks
        SET status = #{status},
            reject_reason = #{rejectReason},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <select id="findByUserIdWithFilters" resultMap="WorkflowTaskResultMap">
        SELECT <include refid="allColumns"/>
        FROM workflow_tasks
        WHERE user_id = #{userId}
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="projectId != null">
            AND project_id = #{projectId}
        </if>
        <if test="agentId != null">
            AND agent_id = #{agentId}
        </if>
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countByUserIdWithFilters" resultType="int">
        SELECT COUNT(*)
        FROM workflow_tasks
        WHERE user_id = #{userId}
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="projectId != null">
            AND project_id = #{projectId}
        </if>
        <if test="agentId != null">
            AND agent_id = #{agentId}
        </if>
    </select>

    <select id="findByProjectId" resultMap="WorkflowTaskResultMap">
        SELECT <include refid="allColumns"/>
        FROM workflow_tasks
        WHERE project_id = #{projectId}
        ORDER BY id DESC
    </select>

</mapper>
