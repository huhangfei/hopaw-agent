<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agent.hopaw.infra.mapper.PromptMapper">

    <resultMap id="PromptResultMap" type="com.agent.hopaw.infra.model.entity.Prompt">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="content" column="content"/>
        <result property="tags" column="tags"/>
        <result property="heat" column="heat"/>
        <result property="userId" column="user_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="allColumns">
        id, name, content, tags, heat, user_id, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.agent.hopaw.infra.model.entity.Prompt">
        INSERT INTO prompts (name, content, tags, heat, user_id)
        VALUES (#{name}, #{content}, #{tags}, 0, #{userId})
        <selectKey resultType="long" keyProperty="id" order="AFTER">
            SELECT last_insert_rowid()
        </selectKey>
    </insert>

    <update id="update" parameterType="com.agent.hopaw.infra.model.entity.Prompt">
        UPDATE prompts
        SET name = #{name},
            content = #{content},
            tags = #{tags},
            update_time = datetime('now','localtime')
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <delete id="deleteById">
        DELETE FROM prompts WHERE id = #{id}
    </delete>

    <select id="findById" resultMap="PromptResultMap">
        SELECT <include refid="allColumns"/>
        FROM prompts
        WHERE id = #{id}
    </select>

    <select id="findByUserId" resultMap="PromptResultMap">
        SELECT <include refid="allColumns"/>
        FROM prompts
        WHERE user_id = #{userId}
        <if test="keyword != null and keyword != ''">
            AND (name LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="tag != null and tag != ''">
            AND (tags = #{tag} OR tags LIKE CONCAT(#{tag}, ',%') OR tags LIKE CONCAT('%,', #{tag}, ',%') OR tags LIKE CONCAT('%,', #{tag}))
        </if>
        <choose>
            <when test="sortBy == 'createTime'">ORDER BY create_time DESC</when>
            <otherwise>ORDER BY heat DESC, create_time DESC</otherwise>
        </choose>
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM prompts
        WHERE user_id = #{userId}
        <if test="keyword != null and keyword != ''">
            AND (name LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="tag != null and tag != ''">
            AND (tags = #{tag} OR tags LIKE CONCAT(#{tag}, ',%') OR tags LIKE CONCAT('%,', #{tag}, ',%') OR tags LIKE CONCAT('%,', #{tag}))
        </if>
    </select>

    <update id="incrementHeat">
        UPDATE prompts SET heat = heat + 1 WHERE id = #{id}
    </update>

    <select id="findAllTags" resultType="java.lang.String">
        SELECT DISTINCT tags FROM prompts
        WHERE user_id = #{userId} AND tags IS NOT NULL AND tags != ''
    </select>

</mapper>
