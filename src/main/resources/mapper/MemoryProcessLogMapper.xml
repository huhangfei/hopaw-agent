<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agent.hopaw.mapper.MemoryProcessLogMapper">

    <select id="getLastProcessedChatId" resultType="java.lang.Long" parameterType="string">
        SELECT ifnull(last_processed_chat_id, 0)
        FROM memory_process_log
        WHERE `identity` = #{identity}
    </select>

    <insert id="upsert">
        INSERT INTO memory_process_log (identity, last_processed_chat_id, last_processed_time)
        VALUES (#{identity}, #{chatId}, CURRENT_TIMESTAMP)
        ON CONFLICT(identity) DO UPDATE SET
            last_processed_chat_id = excluded.last_processed_chat_id,
            last_processed_time = excluded.last_processed_time
    </insert>

</mapper>
