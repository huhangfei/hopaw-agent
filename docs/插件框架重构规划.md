# 插件框架重构规划 v2：以 AgentPlugin 为主体的分层插件体系（无兼容负担版）

> 状态：**S0–S6 已全部实施完成（2026-09-20）**，剩 §6 端到端运行时验收待真实环境跑通。
> **前提定案（2026-09-20）**：不考虑历史兼容。部署时完全重新初始化——`agent.db`（含 SysConfig、Agent 绑定）、`plugins/` 目录、插件仓库数据全部从零开始。
> 本版基于该前提对 v1 规划（含其 §8 复核结论）做全面简化：所有为兼容而设的机制一律删除。
> 关联文档：`docs/插件开发说明.md`（现行体系，已按 AgentPlugin 主体模型重写）
> 涉及模块：`hopaw-contract`、`hopaw-infra`、`hopaw-app`、`hopaw-plugin-repo`、`tools/*`

---

## 0. 前提变更带来的简化总览

v1 规划及其复核（§8）中的下列机制**全部作废删除**：

| # | v1 中的兼容机制 | 作废理由 | 处置 |
|---|----------------|----------|------|
| C1 | **LegacyAgentPluginAdapter**（无 AgentPlugin 的 JAR 自动包装，13 个存量插件"零改动运行"） | 存量插件将随重新初始化一并迁移 | 删除。加载器**强制要求** JAR 内有显式 `AgentPlugin` 实现，没有即拒绝安装 |
| C2 | **配置前缀硬约束**（`tool.<name>.` 永久不可变 + 插件级另立 `plugin.<id>.*` 双轨） | SysConfig 全部重来，无存量键可丢 | 不再是"硬约束"，仅是设计选择（见 §3.2） |
| C3 | **接口方法只 @Deprecated 不物理删除**（AgentTool 上的元数据/生命周期/invoke 降级保留） | 无存量源码需要继续编译 | AgentTool **彻底瘦身**，多余方法物理删除 |
| C4 | **legacyNames 别名**（pluginId 改名后商店"已安装"误判） | 无存量安装状态 | 删除。pluginId 自由命名 |
| C5 | **仓库清单 v1 自动包装为 v2** | 仓库数据从零开始 | 仓库直接只认 v2 清单 |
| C6 | **assets 路由双 key 回退**（pluginId 未命中回退 jarFileName） | 无已缓存页面/客户端 | 直接切换为 pluginId |
| C7 | **旧 HTTP 端点保留转发**（`/tools/api/unload` 等共存） | 无旧前端/客户端 | 端点直接替换，不保留旧路由 |
| C8 | **IAgentToolService 外观 + 委派过渡**（12 个注入点不敢动签名） | 接口签名可以随便改 | 直接拆分重命名，注入点一次性更新 |

**作废后的净收益**：代码量预估减少约 1/3（适配器、双轨、别名、回退、过渡端点全免），接口设计干净，无"新旧两套口径"并存的过渡态。

**新增代价（唯一的）**：13 个存量插件从"可选迁移"变为**强制全量迁移**——每个插件需补一个 `AgentPlugin` 实现类。为此提供 `AbstractAgentPlugin` 基类，把单工具插件的迁移成本压到约 30 行样板代码（详见 §6 S6）。

---

## 1. 现状研究结论（不变，仍有效）

### 1.1 现行加载与注册链路

```
plugins/*.jar
  └─ JarPluginLoader（启动扫描 + 热装入口）
      └─ PluginClassLoader.scanAgentToolClasses()   ← 扫描 JAR 内所有 AgentTool 实现类
          └─ 逐个 newInstance() + autowireBean() + asyncInit()
              └─ DynamicToolRegistry.register(jarFileName, classLoader, List<AgentTool>)
                  └─ PluginEntry（key = JAR 文件名，含 tools 列表 + plugin-assets.json 解析结果）
```

消费侧：

- `AgentToolService.getToolSets()`：内置 Bean（`getBeansOfType(AgentTool.class)`）+ 插件工具，逐个 AgentTool 生成一个 `ToolSetInfo`（扫描类上的 `@Tool` 方法）；
- `AgentService.getToolSetFromAgent()`：按 `Agent.tools`（逗号分隔的**工具集名**）过滤 `ToolSetInfo`，交给 `AgentExecutor`；
- 前端 `tools.html` 平铺展示 `ToolSetInfo` 列表（卸载/导出按钮挂在每个工具集上）；
- 插件仓库 `hopaw-plugin-repo`：`packages/<name>/<version>/` 目录 + 导出清单 `PluginExportInfo`（继承自 `ToolSetInfo`）。

### 1.2 问题一结论：每个插件是不是只能注册一个 AgentTool？

**技术上不是，事实上一却是。**

- 加载器本身支持多个：`scanAgentToolClasses()` 返回 List，一个 JAR 里有 N 个 AgentTool 类会全部实例化并注册；
- 但**整个管理层都建立在"1 JAR = 1 AgentTool = 1 ToolSet"的假设上**，多工具 JAR 是"半残支持"：
  - 冲突检测 `detectConflicts()` 用 `entry.getTools().get(0).getName()` 当插件名（`AgentToolService` L370）；
  - 升级判断 `previousVersion = existing.getTools().get(0).getVersion()`（L513/L573/L590，共 3 处 `get(0)` 假设）；
  - 导出只带一个工具集的元数据，同一 JAR 的其余工具集元数据丢失；
  - `tools.html` 平铺展示，多工具 JAR 会出现多个指向同一 JAR 的卸载按钮；
  - 配置只有工具级，插件级公共配置（如"数据库插件"给 3 个工具共用的连接信息）无处安放。
- 现实佐证：`tools/` 下 13 个插件模块**全部**只含一个 AgentTool 类。

### 1.3 其他结构性问题（与兼容无关，重构必须解决）

| # | 问题 | 代码位置 |
|---|------|----------|
| P1 | 插件身份寄生在工具上：name/version/author/url/icon/keyword 定义在 `AgentTool` 接口 | `AgentTool.java` |
| P2 | 注册表 key 是 JAR 文件名而非插件标识：改名 JAR 即视为不同插件 | `DynamicToolRegistry` |
| P3 | "插件"存在两套口径：前端资产是 JAR 级（plugin-assets.json），invoke/WebSocket 指令却是工具集级 | `PluginEntry` / `PluginInvokeController` |
| P4 | 导出/仓库清单以工具集为主体：`PluginExportInfo extends ToolSetInfo` | `PluginExportInfo` / `PluginRepoService` |
| P5 | LLM 管理工具按工具集名操作，与"装的是插件"错位 | `ToolManagerTool` |
| P6 | 卸载时 `cleanConfig` 只清被点中的那一个工具集的配置，多工具插件会残留 | `AgentToolController.unloadPlugin` |
| P7 | 前端联动缺口：智能体表单平铺 checkbox 依赖直接子元素选择器、`plugin-store.js` 用字符串比较判版本（1.10.0 < 1.9.0 误判）、`index.js` 缺 toolSet→plugin 映射 | `agent-form-fragments.html` / `plugin-store.js` / `index.js` |

### 1.4 可直接继承的资产

- `PluginEntry` + `PluginClassLoader`：JAR 级生命周期（classloader 隔离、资源缓存、临时文件清理）完备，无需大改；
- `plugin-assets.json` + `PluginAsset` + `PluginAssetController` + 前端 `plugin-loader.js`/`plugin-hook.js`：前端组件注入体系天然插件级，直接平移；
- `@Tool` 方法扫描（`scanToolSet`）、安全级别、审批链路：工具级机制成熟，保持不动；
- `Agent.tools` 按工具集名绑定的粒度：设计合理，保留（这不是兼容妥协，而是"智能体使用以 ToolSet 为主"的正确抽象）。

---

## 2. 目标架构：AgentPlugin 主体模型

### 2.1 核心思想

把"插件"从 AgentTool 的一个侧面，提升为**独立的一等公民接口**，并且**由插件充当工具的工厂**：

```
AgentPlugin（插件主体，1 JAR = 1 实例）
 ├── 插件元数据：id / name / version / author / url / description / icon / keyword
 ├── 插件生命周期：asyncInit() / destroy() / onConfigChanged()
 ├── 插件级配置：getConfigItems() + 前缀 "plugin.<id>."
 ├── 能力声明 + 工具工厂：getTools() → 0..N 个 AgentTool（插件自建实例，可注入共享资源）
 ├── 前端组件：plugin-assets.json（沿用，jar 级）
 └── 通用调用：invoke(String toolRef, Map<String, Object> params)
```

`AgentTool` **瘦身为纯工具**（物理删除插件身份与插件级能力，无 @Deprecated 过渡）：

```java
public interface AgentTool {
    String getName();            // 工具集名（全局唯一，Agent 绑定、前端 hook 过滤仍用它）
    String getDescription();
    default String getIcon() { return DEFAULT_ICON; }      // 工具集图标（保留：内置工具无插件父节点）
    default String getKeyword() { return ""; }             // 匹配关键字（保留：同上）
    // @Tool 方法 + @ToolSecurityLevel：不变
    default List<ToolConfigItem> getConfigItems() { return List.of(); }   // 工具级配置（保留）
    default String getConfigPrefix() { return "tool." + getName() + "."; } // 框架统一约定（保留）
    default void asyncInit() { }  default void destroy() { }              // 工具级可选钩子（保留）
    default void onConfigChanged() { }
    // 已删除：getVersion/getAuthor/getUrl → AgentPlugin（插件身份）
    // 已删除：invoke(Map) → AgentPlugin（插件级通用调用，签名升级为 invoke(toolRef, params)）
}
```

> **与初稿的偏差（2026-09-20 S2 实施时确认，更优）**：初稿计划把 `getIcon/getKeyword/getConfigPrefix/getConfigItems/asyncInit/destroy` 一并删除，实际保留，理由：
> ① **内置工具**（infra/avatar 共 14 个 @Component Bean，如 `AvatarTool`）**没有插件父节点**，仍需 icon/keyword 展示与生命周期钩子；
> ② 图标/关键字本质是**工具集级**展示元数据（多工具插件的各工具理应有各自图标）；
> ③ `getConfigPrefix()` 被 8 个插件用于读取自身工具级配置，删除无收益；
> ④ 保留工具级 `asyncInit/destroy` 使迁移可以渐进，且不破坏"插件 asyncInit → 工具 asyncInit；工具 destroy → 插件 destroy"的顺序契约。
> **真正属于插件的只有三项**：版本/作者/来源（插件身份）、`invoke`（插件级通道）、插件级配置段——本次已全部上移。

### 2.2 关键设计决策

**D1 - 一个插件 0..N 个工具**：`getTools()` 返回空列表合法——纯前端组件插件（只注入 js/css/html）、纯 invoke 服务插件都是合法形态。加载器**强制要求 JAR 内有显式 `AgentPlugin` 实现**，没有即拒绝安装（纯前端插件也须提供一个 `getTools()` 返回空列表的 AgentPlugin）；同一 JAR 内出现多个 AgentPlugin 实现同样拒绝。

**D2 - 插件标识**：`AgentPlugin.getId()` 即 pluginId，全局唯一，作为注册表主键（替代 JAR 文件名）。JAR 文件名仅降级为条目的物理定位属性（卸载删文件、assets 资源读取用）。加载时同 id 已注册 → 拒绝并提示"插件已安装，请走升级"；安装/升级接口内部按 **pluginId** 覆盖旧版本（新旧 JAR 文件名不同也成立，残留旧 JAR 一并清理），重复检测排除自身旧版本。
**迁移便利约定（已实施）**：现有单工具插件迁移时 `getId()` **沿用原工具集名**（`getName()` 原值），这样前端 invoke 路由 `/api/plugins/{pluginId}/invoke`、插件商店的"已安装"判定与既有 UI 口径零变化。

**D3 - 工具命名空间**：`@Tool` 工具名保持全局唯一（沿用冲突检测），约定 `<pluginId>_<method>` 前缀（现状已如此：`gomoku_startGame`）。`ToolSetInfo` 增加 `pluginId` / `pluginName` 归属字段（字段可以直接加，无需考虑旧消费方）。

**D4 - 插件即工具工厂（本版新增，取代 v1 的 Legacy 适配）**：

```
加载：newInstance(AgentPlugin) → autowireBean(插件) → 插件.asyncInit()
      （asyncInit 内构建共享资源：连接池、HttpClient 等）
      → 插件.getTools() 取工具实例 → 逐个 autowireBean(工具) → 回填宿主 pluginId → 注册
      （回填须在工具 asyncInit 之前：asyncInit 内可能已按 getConfigPrefix() 读配置）
销毁：插件.destroy()（插件负责释放自己创建并分发给工具的全部资源）→ 关闭 classloader
```

- 多工具插件共享连接池的时序问题在**设计上消失**：资源由插件先建、工具后拿，不存在"工具先于插件 init"的可能；
- 工具不再有生命周期方法，接口和心智模型都更简单；
- 迁移样板收敛到 `AbstractAgentPlugin` 基类（见 §6 S6）。

**D5 - 接口契约**（hopaw-contract 新增）：

```java
public interface AgentPlugin {
    String getId();                          // 插件唯一标识
    String getDescription();
    default String getName()    { return getId(); }
    default String getVersion() { return "1.0.0"; }
    default String getAuthor()  { return "Agent Plugin"; }
    default String getUrl()     { return "https://gitee.com/hgflydream/hopaw-agent"; }
    default String getIcon()    { return "plugin.svg"; }   // jar 内 static/icons/plugins/
    default String getKeyword() { return ""; }
    default void asyncInit()    { }
    default void destroy()      { }
    default List<ToolConfigItem> getConfigItems() { return List.of(); }
    default void onConfigChanged() { }
    List<AgentTool> getTools();              // 0..N，asyncInit 后调用
    default Map<String, Object> invoke(String toolRef, Map<String, Object> params) {
        throw new UnsupportedOperationException("插件未提供 invoke 能力");
    }
}
```

`invoke` 的 `toolRef` 用于多工具插件的路由（传工具集名；为空时由插件自行决定默认行为）。v1 复核中"invoke 委托 tools[0] 导致第二工具不可达"的问题在显式 AgentPlugin 下不存在——路由权在插件自己手里。

---

## 3. 管理层级拆分

### 3.1 两级模型

```
一级：插件管理（PluginRegistry / PluginManagerService）
 ├── 安装 / 升级 / 卸载（以 pluginId + version 为准）
 ├── 启用 / 禁用（新增运行时状态，禁用 = 工具不进 ToolSets、前端资产不注入）
 ├── 插件级配置（plugin.<id>.* 配置页）
 └── 插件元数据展示（版本/作者/来源/前端资产数/工具数）

二级：插件内工具管理（ToolSetService）
 ├── 工具集（AgentTool）列表 + @Tool 方法明细（参数/描述/安全级别）
 ├── 工具级配置（沿用 tool-config 模板）
 └── 工具集启停（可选：粒度到单个 AgentTool）
```

服务层拆分（现 `AgentToolService` 职责过载，一并拆解；**不再需要外观委派过渡**，直接改）：

| 现类 | 拆分后 | 职责 |
|------|--------|------|
| `AgentToolService` | `PluginManagerService` | 插件安装/升级/卸载/启停/导出、冲突检测（12 个 `IAgentToolService` 注入点一次性更新，顺手消灭 `ToolConfigController` 直接依赖实现类的坏味道） |
| `AgentToolService` | `ToolSetService` | ToolSet 扫描（`scanToolSet` 平移）、按 agent 组装 `List<ToolSetInfo>`、`getToolNameAndDescriptionMap` |
| `DynamicToolRegistry` | `PluginRegistry` | key = pluginId；`PluginEntry` 持有 `AgentPlugin` 实例 + `List<AgentTool>` + assets + jar 物理路径 |

### 3.2 配置两级化（设计选择，非兼容约束）

重新初始化后配置键无存量包袱，但仍采用**两个独立前缀段**，理由是简单清晰而非兼容：

- **插件级**：`plugin.<pluginId>.*`，`AgentPlugin.getConfigItems()` 声明，用于多工具共享的公共配置（连接信息、凭证等）；
- **工具级**：`tool.<工具集名>.*`，`AgentTool.getConfigItems()` 声明——工具集名本就全局唯一，`ToolConfigService` 与 `tool-config` 页面逻辑可原样复用；
- `ConfigChangeEvent` 分发：`plugin.<id>.` 前缀 → 插件 `onConfigChanged()`；`tool.<name>.` 前缀 → 对应工具 `onConfigChanged()`；
- 前缀由**框架统一约定**：`AgentTool` 保留 `getConfigPrefix()`（返回 `tool.<工具集名>.`），插件级前缀由 `PluginConfigService.prefix(pluginId)` 统一计算为 `plugin.<id>.`（`AgentPlugin` 接口不设前缀方法），插件通过 `IPluginConfigService.get(id, key)` / `IToolConfigService.get(name, key)` 读配置——消灭 8 个插件各自拼 key 的散装读法；
- 前端配置页：一级页面选插件，二级页内分「插件配置」区块 + 每个工具集的「工具配置」区块。

### 3.3 智能体使用侧：仍以 ToolSet 为主体（设计保留，非兼容妥协）

- `Agent.tools` 仍存工具集名，`AgentExecutor` 消费 `List<ToolSetInfo>` 的链路不动；
- 变化在展示与选择体验：智能体编辑页的工具选择列表按**插件分组**展示（插件 → 工具集两级树），勾选粒度仍是工具集；
- `ToolSetInfo` 新增 `pluginId`、`pluginName` 字段，`/tools/api/list` 响应直接扩展（无需保留旧字段口径的顾虑，但旧字段本身有用，保留）。

### 3.4 HTTP API（直接替换，不保留旧路由）

| 现端点 | 新端点 / 变化 |
|--------|--------------|
| `GET /tools/api/list` | 响应增加 `pluginId/pluginName`；另增 `GET /plugins/api/list`（插件级聚合视图） |
| `POST /tools/api/unload`（toolName+toolVersion） | **替换为** `POST /plugins/api/unload`（pluginId） |
| `GET /tools/api/export/{toolName}/{toolVersion}` | **替换为** `GET /plugins/api/export/{pluginId}`（导出整插件包：清单 v2 + JAR，下载文件名 `<pluginId>-<version>.zip`） |
| `POST /api/plugins/{toolName}/invoke` | **替换为** `POST /api/plugins/{pluginId}/invoke`，body 含 `toolRef` |
| `GET /api/plugins/assets` | 不变（本就是插件级），`plugin` 字段语义由 JAR 文件名改为 **pluginId** |
| `GET /api/plugins/{plugin}/assets/**` | 路径变量改为 pluginId，直接切换 |
| WebSocket `cmd.toolName` | 口径无需改动：插件 id 沿用原工具集名，故 `cmd.toolName` 即 pluginId，前端 `PluginHook.callTool(pluginId)` 直接命中 `/api/plugins/{pluginId}/invoke` |

### 3.5 卸载与清理语义（修复现状 P6）

卸载以 pluginId 为单位，`cleanConfig` 必须清空 `plugin.<id>.*` 全部键：

1. 插件级配置：`plugin.<id>.<key>`（含 MAP 散键）；
2. 插件下**所有**工具集的配置：`plugin.<id>.tool.<工具集名>.<key>`（含 MAP 散键）——已在本前缀之下，故实现上先按声明项逐个 `deleteAll`，再按 `plugin.<id>.` 做一次前缀兜底扫描（覆盖已从代码中移除的历史配置项）。

导出同理：导出整插件（pluginId），不再接受 toolName/version 维度。

### 3.6 LLM 管理工具（ToolManagerTool → PluginManagerTool）

`plugin` 工具集升级为按插件粒度描述能力（"安装/卸载/列出插件，插件可含多个工具集"），`@Tool` 方法参数从工具名改为插件名/包路径。

---

## 4. 插件仓库分层

### 4.1 清单格式 v2（以 AgentPlugin 为主体，唯一格式）

仓库**只认 v2**，无 v1 包装逻辑：

```json
{
  "manifestVersion": 2,
  "id": "database",
  "name": "数据库插件",
  "description": "提供数据库查询与结构管理能力",
  "version": "2.0.0",
  "author": "xxx",
  "url": "https://gitee.com/hgflydream/hopaw-agent",
  "icon": "<svg ...>",
  "keyword": "数据库,SQL,查询",
  "jarFileName": "hopaw-tool-database-2.0.0.jar",
  "fileSize": 123456,
  "sha256Hash": "…",
  "provides": [
    {
      "name": "databaseQuery",
      "description": "SQL 查询工具集",
      "toolCount": 2,
      "methods": [
        { "name": "databaseQuery_executeSql", "description": "执行 SQL 查询语句" },
        { "name": "databaseQuery_listTables", "description": "列出数据库中的表" }
      ]
    },
    { "name": "databaseSchema", "description": "结构管理工具集", "toolCount": 3, "methods": [ … ] }
  ],
  "frontendAssetCount": 2,
  "frontendAssets": [
    { "name": "panel.js", "path": "static/panel.js", "type": "js", "size": 8123 },
    { "name": "panel.css", "path": "static/panel.css", "type": "css", "size": 2140 }
  ],
  "invokeSupport": true,
  "configItems": [ … ]
}
```

清单字段与 `PluginPackageManifest` 一一对应、**没有嵌套包装**：`provides` 是数组，前端资产数与 invoke 能力是与它平级的 `frontendAssetCount` / `invokeSupport`。

**明细字段（导出即带，商店/仓库页据此展示）**：

- `provides[].methods`：该工具集所有 `@Tool` 方法的 `name` + `description`（按名称排序，导出稳定）；`toolCount` 即其长度。**旧版导出的清单可能没有该字段**，消费方必须容错（前端降级显示「无方法明细」）。
- `frontendAssets[]`：每个前端资产的 `name`（JAR 内路径末段）、`path`（JAR 内完整路径）、`type`（js/css/html）、`size`（JAR 条目的未压缩字节数，读不到时为 `-1`，前端显示「未知」）。`frontendAssetCount` 与之长度一致，保留作快速摘要。
- 方法明细来自**运行中的插件实例**（`buildManifest` 反射 `@Tool` 注解），资产大小来自**JAR 条目**（`JarFile#getEntry#getSize`），二者都不需要解包。

**「清单 v2」这个说法的由来**：`manifestVersion` 标识的是**清单格式代次**，不是插件自身的版本号（插件版本是同一份清单里的 `version`）。导出包内的清单文件名为 `<jar 基名>.json`（见 `PluginManagerService.buildManifest` / `exportPlugin`）。两条链路都**只接受 v2**（`PluginManagerService.validateManifest` / `PluginRepoService.validateManifest` 校验 `manifestVersion == 2`），该字段**不在页面上展示**——它恒为 2、对用户没有信息量（商店与仓库管理页早期曾渲染过一个「清单 v{n}」徽标，已移除）。

对照：**v1 是重构前的旧格式**（`PluginExportInfo extends ToolSetInfo`），以**工具集**为主体——一个条目就是一个工具集、`name` 承载"首个工具集名"的语义，且没有 `manifestVersion` 字段；v2 改为以**插件（AgentPlugin）**为主体（元数据 + 0..N 工具集 + 可选前端资产 + 可选 invoke + 插件级配置项）。按 §0 的 C5 处置，仓库**不做** v1→v2 包装，直接只认 v2，故清单格式版本当前恒为 2、无需在界面上呈现。

仓库目录结构 `packages/<pluginId>/<version>/` 不变（本就是插件级目录，只是条目 name 的语义从"首个工具集名"修正为 pluginId）。

### 4.2 仓库与商店的分层展示

- `PluginRepoService.scanPlugins()` 条目从"工具集"改为"插件"：一个条目 = 基本信息 + N 个版本，每版本声明 `provides`；
- `plugin-repo.html` 左侧列表**只展示插件**（不再展开版本子树），版本收进详情页的**版本下拉**：语义化降序（从新到旧）、最新版标注「（最新）」、仅一个版本时置灰禁用，切换下拉即整块详情切换；详情上方字段区同步收紧间距（图标 44px、字段卡 padding 8/12、栅格 gap 10px，SHA256 单行省略 + `title` 悬停看全文）；
- `plugin-store.html` 卡片改为插件主体（插件名/图标/描述 + 工具集徽标 + 前端组件徽标），版本列表不变；
- "已安装判断"从 toolName+version 改为 **pluginId+version**（重新初始化后不存在别名匹配问题）；
- 顺手修复 `plugin-store.js` 的**字符串版本比较 bug**（`1.10.0 > 1.9.0` 误判），改为语义化版本比较。

### 4.3 仓库服务端（hopaw-plugin-repo）

`PluginRepoController` / 管理页的"导入插件包"校验逻辑只读 v2 清单；`PluginRepoResult` DTO 增加层级结构（插件 → 版本 → provides）。

**实现要点（2026-09-20 落地）**：
- 仓库目录名以**清单 `id`** 为准（目录名与清单 id 不一致时记 warn 并以清单为准），不再把目录名当"首个工具集名"用；
- 扫描链路对非 v2 / 缺 id / 无能力的版本目录**跳过并记日志**，导入链路则直接抛错并给出可读原因（清单版本不支持 / 缺 id / 未声明能力 / JAR 哈希不匹配）；
- 版本排序与"是否有新版本"判定统一走 `SemVer`（仓库 Java 侧 + 商店 JS 侧各一份同规则实现），杜绝 `1.10.0 < 1.9.0` 类误判；
- 商店版本状态细分为 `installed` / `update_available` / `older` / `not_installed`，低版本不再被提示为"可更新"，而是"回退"。

---

## 5. 实施阶段划分

v1 的"S2.5 低风险身份修复""S3' 拆分放最后"等排序技巧都是为兼容过渡设计的，现在**不需要**——可以直接按依赖顺序线性推进：

| 阶段 | 模块 | 内容 |
|------|------|------|
| **S0 决策定案** | — | **✅ 已完成（2026-09-20）**：§7 四问全部定案 |
| **S1 契约层** | hopaw-contract | **✅ 已完成（2026-09-20）**：`AgentPlugin` + `AbstractAgentPlugin` 基类 + `PluginDescriptor` DTO + `IAgentPluginService`。**纯增量，不改 AgentTool**（瘦身挪到 S2，保证 S1 单独可编译） |
| **S2 加载与注册** | hopaw-infra | **✅ 已完成（2026-09-20）**：`PluginClassLoader.scanAgentPluginClasses()`；`DynamicToolRegistry` → `PluginRegistry`（key=pluginId，同 id 拒绝共存）；`JarPluginLoader` 强制 AgentPlugin + 插件即工厂（插件 asyncInit → getTools → 逐个 autowireBean + asyncInit）+ 卸载逆序（工具 destroy → 插件 destroy）；`AgentTool` 删除 version/author/url/invoke；`AgentToolService` 插件化（消灭 3 处 `get(0)`、ToolSetInfo 增加 pluginId/pluginName、图标与关键字按插件回退）；`PluginInvokeController` 切到 `/api/plugins/{pluginId}/invoke`（body 支持 toolRef）；**15 个插件模块全部迁移**（各新增 XxxPlugin，含 gomoku invoke 上移到插件级）；全量编译 21 模块 BUILD SUCCESS |
| **S3 服务与 API** | hopaw-infra + hopaw-app | **✅ 已完成（2026-09-20）**：契约层删除 `IAgentToolService`，新增 `IToolSetService`（二级）与 `IPluginConfigService`，`IAgentPluginService` 扩展为插件一级管理（install/upgrade/unload/export/toggle/config/invoke）；`AgentToolService` 拆为 `PluginManagerService` + `ToolSetService`（12 个注入点全部更新，全仓库已无旧引用）；`ConfigItemStore` 抽公共配置存取（MAP 结构 JSON + 散键清理），`PluginConfigService` 走 `plugin.<id>.*` 前缀，`ToolConfigService` 改走 `tool.<name>.*`；插件启停 `plugin_state` 表 + `PluginStateMapper/Service`（内存缓存 + 表未就绪容错）+ ToolSet 组装过滤；`PluginController` 承接 `/plugins/api/list|config-info|toggle|unload|export|install-upgrade|local-install|config/{pluginId}`，`AgentToolController` 只留工具集与 `/api/toolset-config-info`；`PluginAssetController`/`PluginInvokeController` 改按 pluginId 并校验禁用；`ToolManagerTool` → `PluginManagerTool`（按插件粒度）；`PluginPackageManifest` v2（provides 工具集清单 + configItems）；卸载遍历清理插件下全部工具集配置；全量编译 22 模块 BUILD SUCCESS |
| **S4 前端 UI** | hopaw-app | **✅ 已完成（2026-09-20）**：`tools.html` 左侧两级管理树（插件一级 → 其工具集二级）+「插件 / 内置工具」双 Tab（Q1）+ 搜索按插件/工具集命中；右侧新增插件详情（标识/版本/作者/关键词/前端资产/invoke/包文件 + 启停 + 插件配置 + 导出 + 卸载）与工具集详情（含「返回插件」）；新增插件级配置页 `plugin-config.html`（配置表单抽为 `fragment/config-form` 与工具配置页共用）；`tools.js` 端点全部迁到 `/plugins/api/*`（config-info?pluginId、unload(pluginId)、export/{pluginId}、local-install）+ 新增 togglePlugin/switchToolTab/selectPluginById；智能体编辑页按插件分组（`fragment/tool-checkbox` 复用），禁用插件工具集置灰并标注「插件已禁用，暂不可用」，编辑态保留原勾选（不禁用复选框）以便重新启用后恢复，新增态禁用复选框，全选/取消跳过禁用项；禁用插件的前端资产由 `PluginAssetController` 过滤，无需前端 unload；**WebSocket 指令口径无需改动**（插件 id 沿用原工具集名，`cmd.toolName` 即 pluginId，前端 `PluginHook.callTool(pluginId)` 直接命中 `/api/plugins/{pluginId}/invoke`） |
| **S5 仓库** | hopaw-plugin-repo | **✅ 已完成（2026-09-20）**：契约层新增 `SemVer`（语义化版本比较，主版本逐段数值 + 预发布标识），`PluginRepoResult` 重写为插件主体（`id`=pluginId / `name`=展示名 + `VersionEntry.provides` / `frontendAssetCount` / `invokeSupport` / `configItemCount`），删除 v1 遗留 `PluginExportInfo`、`PluginToolRef`；仓库服务端只认 v2 清单（`manifestVersion=2` + `id` 非空 + `hasAnyCapability()`，不通过则跳过并记日志；导入链路直接报错），`packages/<pluginId>/<version>/` 目录按清单 id 落盘，版本语义化降序；仓库管理页改为插件主体展示（标识 / 工具集徽标 / 前端资产 / invoke / 每版本 provides 摘要）；Agent 侧商店已安装判定改为仅 pluginId，版本状态用 SemVer 区分 `installed` / `update_available` / `older`（旧代码把低版本误判为"可更新"），`plugin-store.js` 字符串版本比较 bug 修复并改用插件标识寻址；落地 §7-Q3② 纯前端插件安全确认（商店安装纯前端插件需显式确认，`PluginUpdateInfo.allowFrontendOnly`）。**前置的模块构建问题已不复现**：`mvn -pl hopaw-plugin-repo -am package` 与全量 `mvn clean package -DskipTests` 均 21 模块 BUILD SUCCESS |
| **S6 全量插件迁移** | tools/*、docs | **✅ 已完成（2026-09-20）**：① 单工具插件在 S2 已同步交付（15 个活跃模块 + webpage-playwright 各补 `XxxPlugin` 主体）；② **多工具集 + 共享资源上移示范**：`hopaw-tool-database` 拆为 `databaseQuery`（4 个 @Tool：连接测试 / SELECT / DML-DDL / 导出 CSV）+ `databaseSchema`（4 个 @Tool：加载驱动 / 已加载驱动 / 表清单 / 表结构）两个工具集，`JdbcDriverRegistry` 上移插件级由两个工具集共享，旧单工具类 `DatabaseTool` 已删除；③ **零工具集纯前端插件示范**：新增 `tools/hopaw-tool-chat-beautify`（会话页背景色设置），**未改动 canvas 插件**（按用户指示）；④ **Q3③ 沙箱安全策略落地**：`plugin-assets.json` 新增 `sandbox` / `sandboxApis` 声明 → `PluginAsset` / `PluginRegistry` / `PluginAssetController` 全链路透传 → `plugin-loader.js` 用 `<iframe sandbox="allow-scripts">` 隔离注入 + 宿主白名单能力桥（`ui.resize` / `theme.setBackground` / `theme.getBackground`）；⑤ **图标 404 修复**：新增 `PluginIconResolver`，`PluginManagerService` / `ToolSetService` 从 JAR 内联 SVG 下发，仓库侧导入/扫描时就地解包 zip→jar 读图标并缓存；⑥ 《插件开发说明.md》按 AgentPlugin 主体模型全面重写（含沙箱章节、两级配置、多工具集配方、端点表）。**验证**：全量 `mvn -B -DskipTests compile` **22 模块 BUILD SUCCESS**。**遗留**：§6 端到端运行时验收（需真实启动应用 + 真实插件包逐项跑通） |
| **S7 插件/工具管理拆分 + 三级禁用** | hopaw-contract + hopaw-infra + hopaw-app | **✅ 已完成（2026-09-20）**：**页面拆分**——新增「插件管理」运维页 `plugins.html`（`/plugins`，插件平铺列表 + 详情内工具集手风琴默认收起，承接启停/配置/导出/卸载/商店/本地安装），`op-tabs` 增「插件管理」Tab、侧边栏系统运维菜单高亮 `plugins`；`tools.html` 瘦身为「工具管理」（全部工具集平铺列表 + 详情含工具集级/方法级开关），`AgentToolController` 不再依赖 `IAgentPluginService`；**三级禁用**——① 状态层：`ToolState` 实体 + `ToolStateMapper(+xml)` + `DatabaseInitializer` 建 `tool_state` 表（`toolSetName` PK + `toolName` 默认空串，空串=工具集级）+ `ToolStateService`（内存缓存 + 表未就绪容错）；② 执行过滤：`ToolSetService.scanToolSet` 给 `ToolSetInfo.enabled`/`ToolInfo.enabled` 打标记，`AgentService.getToolSetFromAgent()` 过滤工具集级禁用，`AgentExecutor` 用 `buildEnabledToolMap()`（反射扫描 `@Tool` 排除禁用方法）替代 `aiBuilder.tools()`；③ 开关 UI：`ToolStateController`（`/api/tool-state/toolset`、`/api/tool-state/tool`），插件页手风琴 + 工具页详情 + 智能体表单均接入开关，禁用项仍展示并标注「已禁用」；卸载插件时 `PluginManagerService` 清理其工具集 `tool_state`。**验证**：`mvn -B -DskipTests -pl hopaw-app -am compile` BUILD SUCCESS |

**S6 迁移配方**（每个插件约 30 行样板）：

1. 新增 `XxxPlugin extends AbstractAgentPlugin`：搬入原 `getName/getVersion/getAuthor/...` 元数据（**插件 id 用最短名词，不带 `Tool`/`Plugin` 后缀**，如 `ssh`/`http`/`file`/`command`；工具集名同样去 `Tool` 后缀，工具名统一 `<工具集名>_<method>`——详见《插件开发说明》§3.3）；
2. 原 `asyncInit()` 里建资源/连接池的代码上移到插件的 `asyncInit()`；
3. 插件 `getTools()` 返回原 AgentTool 实例（`new SshTool(连接池)` 或直接 `new SshTool()` + autowire 兜底）；
4. 原 AgentTool 类删掉元数据方法与生命周期方法，只留 `getName/getDescription/@Tool 方法/getConfigItems`；
5. 插件自读配置处改用 `IPluginConfigService`/`IToolConfigService`，删掉 `getConfigPrefix()` 拼接。

迁移顺序建议：demo（S2 冒烟）→ database（演示"1 插件多工具集"）→ canvas（演示"前端组件 + 工具"）→ 其余批量。

---

## 6. 验收标准

- [ ] 一个 JAR 内实现 1 个 AgentPlugin + 2 个 AgentTool：插件管理页显示 1 个插件、2 个工具集；导出/升级/冲突检测均以插件为单位；
- [ ] 一个**无任何 AgentTool**、仅含 plugin-assets.json 的纯前端 JAR：可安装、资产正常注入（沙箱容器）、可卸载；
- [ ] 13 个插件全部迁移完成，逐一通过功能清单：加载、@Tool 调用、配置读写、前端注入、导出/安装、invoke（如原支持）；
- [ ] 卸载插件后，`plugin.<id>.*`（含其下全部工具集的 `plugin.<id>.tool.<工具集名>.*`）配置键被清干净（含 MAP 散键）；
- [ ] 智能体编辑页按插件分组选择工具，`Agent.tools` 存储格式不变（工具集名）；
- [ ] 仓库全新初始化后仅 v2 清单可用：上传、列表、安装、升级、已安装/可更新判定均正确（含 1.10.0 vs 1.9.0 版本比较）；
- [ ] 无 AgentPlugin 实现的 JAR（含"有前端资产但缺 AgentPlugin"）被拒绝安装并给出明确报错。
- [ ] 三级禁用逐级生效：插件级禁用 → 工具集不进列表、前端资产不注入；工具集级禁用 → 智能体不可选/不可执行；方法级禁用 → 该 `@Tool` 方法不参与 AI 调用（重启后状态保持）。

**验收进展（2026-09-20）**：S1/S2/S3/S4/S5/S6/S7 已完成并通过编译、页面渲染与构建校验（全量 `mvn -B -DskipTests compile` **22 模块 BUILD SUCCESS**；S5 阶段全量 `mvn clean package -DskipTests` 21 模块 BUILD SUCCESS；tools / plugins / plugin-config / tool-config / 智能体表单片段 / plugin-repo / api-docs 用真实 DTO 渲染通过）。上表为**端到端运行时验收清单**，需真实启动应用后用真实插件包逐项跑通——尤其是：多工具集插件（`database` 的 `databaseQuery` + `databaseSchema`）、纯前端插件（`chatBeautify` 非沙箱直接注入 → 改背景色/字号 → 刷新还原）、插件安装/升级/导出、卸载配置清理、13 个插件功能冒烟、三级禁用（插件/工具集/方法）逐级验证、以及仓库"上传 → 列表 → 安装 → 升级 → 已安装/可更新判定（含 1.10.0 vs 1.9.0）"全链路。

---

## 7. 决策定案（2026-09-20 用户确认，按建议方案执行）

v1 的 Q2（getId 沿用旧名 + legacyNames）因无兼容而**消失**——pluginId 自由命名；Q3（插件级配置要不要做）因无兼容顾虑，已直接定为**做**（`plugin.<id>.*` 段 + 二级配置页）。剩余 4 问已全部定案：

| # | 问题 | **定案** | 落地阶段 |
|---|------|----------|----------|
| Q1 | 内置工具（hopaw-biz 的 @Component）在两级管理里如何归属？ | **管理页顶层分「插件 / 内置工具」两个 Tab**，内置工具保持平铺展示 | S4 |
| Q2 | 是否引入"插件启用/禁用"状态？禁用后悬空绑定如何处理？ | **做**：`plugin_state` 表（pluginId/enabled/installedVersion）+ ToolSet 组装过滤 + 智能体编辑页提示"工具不可用（插件已禁用）"+ 前端资产显式 unload 或提示刷新 | S3（表与过滤）/ S4（UI 提示） |
| Q3 | 是否正式支持"零 AgentTool 的纯前端插件"？JS 注入权限如何处理？ | **支持**，配套安全策略：① 安装链路强制 sha256 校验（已有，保留）；② 第三方 `pluginSourceUrls` 来源需用户显式确认才允许纯前端插件；③ 纯前端插件的 js 仅注入到指定容器（plugin-assets.json 增加 `sandbox` 声明，默认不放开全局 index 注入） | S2（加载校验）/ S5（仓库侧） |
| Q4 | 允许同一 pluginId 的多个版本 JAR 共存吗？ | **不允许**：同 id 第二个 JAR 拒绝加载并报错，升级走覆盖安装（uninstall → write → load） | S2 |

---

## 8. 风险清单（无兼容版）

1. **迁移期功能回归**：13 个插件强制迁移是本版新增的最大工作量，靠 §6 的逐插件功能清单兜底；建议每迁移一个就跑一遍冒烟，不要攒到最后。
2. **一次性切换无灰度**：旧端点、旧清单、旧指令格式全部直接替换，切换点即 S3/S4/S5 完成时——前后端必须同版本发布，不能交叉部署（对重新初始化的使用方式而言可接受，但需知晓）。
3. ~~**`hopaw-plugin-repo` 既有构建问题**（《插件开发说明.md》§9）：S5 前必须先修，否则无法本地验证。~~ **已排除（2026-09-20）**：实测 `mvn -pl hopaw-plugin-repo -am clean package -DskipTests` 与全量 `mvn clean package -DskipTests` 均 **21 模块 BUILD SUCCESS**，该问题在当前代码/环境下不复现；《插件开发说明.md》§9 对应条目已同步修正。
4. **纯前端插件的安全面**：见 §7 Q3，属安全问题而非兼容问题，不因重新初始化而消失。
5. **插件启停与悬空绑定**：见 §7 Q2，运行时设计问题，同样与兼容无关。
