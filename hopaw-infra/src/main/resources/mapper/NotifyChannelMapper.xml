<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agent.hopaw.infra.mapper.NotifyChannelMapper">

    <resultMap id="NotifyChannelResultMap" type="com.agent.hopaw.infra.model.entity.NotifyChannel">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="type" column="type"/>
        <result property="config" column="config"/>
        <result property="enabled" column="enabled"/>
        <result property="userId" column="user_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.agent.hopaw.infra.model.entity.NotifyChannel">
        INSERT INTO notify_channels (name, type, config, enabled, user_id, create_time, update_time)
        VALUES (#{name}, #{type}, #{config}, #{enabled}, #{userId}, datetime('now','localtime'), datetime('now','localtime'))
        <selectKey resultType="long" keyProperty="id" order="AFTER">
            SELECT last_insert_rowid()
        </selectKey>
    </insert>

    <update id="update" parameterType="com.agent.hopaw.infra.model.entity.NotifyChannel">
        UPDATE notify_channels
        SET name = #{name},
            type = #{type},
            config = #{config},
            enabled = #{enabled},
            update_time = datetime('now','localtime')
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM notify_channels WHERE id = #{id}
    </delete>

    <select id="findById" resultMap="NotifyChannelResultMap">
        SELECT id, name, type, config, enabled, user_id, create_time, update_time
        FROM notify_channels
        WHERE id = #{id}
    </select>

    <select id="findByUserId" resultMap="NotifyChannelResultMap">
        SELECT id, name, type, config, enabled, user_id, create_time, update_time
        FROM notify_channels
        WHERE user_id = #{userId}
        ORDER BY id DESC
    </select>

</mapper>
